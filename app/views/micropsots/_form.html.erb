<%= form_with(model: micropsot, local: true) do |form| %>
  <% if micropsot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(micropsot.errors.count, "error") %> prohibited this micropsot from being saved:</h2>

      <ul>
      <% micropsot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :micropsot_content %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :micropsot_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
